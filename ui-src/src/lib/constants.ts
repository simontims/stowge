/** Time to wait before retrying server requests when they fail. */
export const SERVER_RETRY_DELAY_MS = 5000;

/** Message shown when the Stowge server cannot be reached. */
export const SERVER_UNAVAILABLE_MESSAGE =
  "Cannot reach the Stowge server right now. Check that it is running, then try again.";
